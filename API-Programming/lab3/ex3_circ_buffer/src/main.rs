mod buffer;

fn main() {
    /*
    ● quando head o tail vengono incrementati oltre oltre la lunghezza dell’array tornano a
    zero (è come se l’array fosse chiuso su se stesso in un cerchio)
    ● quando scrivo e tail viene a coincidere con head il buffer è pieno
    API Programming - Esercitazione 3 - 26/28 marzo 2025
    ● quando leggo e head viene a coincidere con tail buffer è vuoto
    ● per gli effetti di queste operazioni in certi momenti tail può essere minore di head e i
    valori nel buffer non sono contigui (una parte in coda e i successivi in cima al buffer)

     */
}
